# ν=1/2 パートン VMC が局所解に留まる件 — 調査レポート

対象ブランチ: `parton-mode` / 正は `DESIGN_parton.md` / 作成: 2026-08-13
**別セッションでのコードレビュー用資料。** 実装提案ではなく「何を測り、何が確定し、
何が未確定か」の記録。

---

## 0. 結論

ν=1/2 で SR が E ≈ −10.9 に留まっていた原因は **変分パラメータ idx を全フレーバーで
共有していたこと**。フレーバー独立にするだけで全シードが改善し、最良シードは
**−16.2612**(ED −16.3049 の 0.27% 上)に到達した。これは参照実装 PartonFCI の
良好ランの値 −16.2610 と一致する。

**これはフィクスチャ(P 層のテスト入力)の作り方の問題であって、本体実装のバグではない。**

ただし **ν=1/3 では逆にフレーバー独立化が悪化させる**(§5-2)。「常にフレーバー
独立にせよ」という一般則にはならず、この非対称は未解決のまま残っている。

副次的に、次の 3 点も確定した。

- 参照実装の SR も、同一 seed で正則化を変えるだけで −7.3 / +0.07 に落ちる。
  「この系 × SR」はもともと多谷・正則化敏感。
- `DSROptRedCut` は現状の犯人ではない(1e-8 と 1e-6 で結果がビット一致 = 1 方向も
  カットされていない)。むしろ 1e-4 まで上げると壊れる。
- **SR ソルバは律速ではない。** 律速は `finalize_oo_store!`(S 行列の蓄積、
  O(N_samp·N_para²))。既存 mVMC の SR-CG がそのまま効く場所。

---

## 1. 参照値と到達値

| 系 | ED 基底 | `:orbit`(従来) 最良 | `:orbit_flavor` 最良 |
|---|---|---|---|
| ν=1/2 ボソン(F=2), 32 サイト, 8 粒子 | **−16.304913354429445** | −16.1724 | **−16.2607** (+0.044, 0.27%) |
| ν=1/3 フェルミオン(F=3), 30 サイト, 5 粒子 | **−10.104174670830064** | **−9.9875** (+0.117, 1.2%) | −9.108(悪化) |

ED 値の出所は `test/physics/test_p0_ed_reference.jl`。P1 で 1 粒子ハミルトニアンが
外部 ED 実装のダンプと 1e-10 一致することを確認済み。

---

## 2. 参照実装 PartonFCI の証拠

外部プロジェクト `/home/nozomihigashino/Meanfields/PartonFCI/` は**読み取りのみ**。
実行はしておらず、既存の実行ログと結果ディレクトリを参照した。

### 2-1. 同一系・同一 seed で SR の正則化だけを変えた 5 本

`partonfci_chi.o1052{1..5}` と対応する `results/vmc_chi/*/run_chi_info.txt`。
すべて Nux,Nuy=4,4 / Ne=8 / K=2 / niter=2200 / NVMCSample=2000 / lr=0.01 /
**seed=42** / SUK_INDEPENDENT(Nchi=92)。

| タグ | gauge_fix | sr_diag_shift (=StaDel) | sr_red_cut (=RedCut) | 最適化 | 最終 E |
|---|---|---|---|---|---|
| `adam_ref_n32` | false | — | — | Adam | **−16.256** |
| `sr_parity_n32` | false | 0.001 | 1e-6 | SR | −7.299 |
| `sr_fb1_gf1` | true | 0.001 | 1e-6 | SR | −7.172 |
| `sr_fb2_gf1_sta` | true | **0.01** | 1e-6 | SR | **−16.261** |
| `sr_fb3_gf1_oldreg` | true | 0.01 | **1e-3** | SR | +0.067 |

1. 参照実装の SR も設定次第で −7 や +0.07 に落ちる。
2. ゲージ固定を入れただけでは救われない(−7.299 → −7.172)。決め手は StaDel を
   0.001 → 0.01 に上げたこと。
3. RedCut を 1e-3 まで上げると壊れる(過剰カット)。

参照実装の `vmc_chi_optimize.jl` 自身が対応表を持つ:
`sr_diag_shift ↔ DSROptStaDel`(乗法シフト `S_ii *= (1+δ)`)、
`sr_red_cut ↔ DSROptRedCut`。**我々の設定(StaDel=0.01, RedCut=1e-8)は
参照が「必須」とした条件を既に満たしている。**

### 2-2. 変分自由度の A/B(参照実装の統制実験、N=128)

`ctrl_unified_n128` と `ctrl_syminit_n128` は niter=3000 / NVMCSample=3000 /
lr=0.003 / seed=42 / gauge_fix=true / StaDel=0.01 / RedCut=1e-6 が完全に同一で、
`symmetry_mode` だけが違う。

| symmetry_mode | Nchi | 最終 E |
|---|---|---|
| SUK_UNIFIED(フレーバー共有) | 46 | −50.714 |
| SUK_INDEPENDENT(フレーバー独立) | 92 | **−65.042** |

到達エネルギーが 28% 変わる。これが候補 C の根拠になった。

---

## 3. idx 粒度の 3 水準とその意味

共通の枠組みは `H_MF^(f)[j,i] = α_{idx(bond,f)} · t_ij`(h.c. は暗黙)、
`Ψ = Π_f det A^(f)`。α は**素のホッピング t_ij に掛かる倍率**で、t_ij 自体は
pmftrans が持ち、pmfpara は「どのボンド×フレーバーがどの α を共有するか」の割り当て表。

`test/physics/parton_fixture.jl` に `idx_mode` を追加した。
**フィクスチャの選択肢を増やしただけで、本体コードは変更していない。**

| `idx_mode` | α の割り当て | 課している対称性 | 指す状態 |
|---|---|---|---|
| `:orbit`(従来・既定だった) | 拡大セル並進で畳んだクラス、**全フレーバー共有** | 拡大セル並進 **＋ フレーバー間の完全な同一性** | H_MF が全フレーバーで同一 → `A^(1)=…=A^(F)` → **Ψ = (det A)^F** |
| `:orbit_flavor` | 同じクラスだが**フレーバーごとに独立** | 拡大セル並進のみ | 各フレーバーが別々の平均場状態を持てる。**参照の SU(K)_INDEPENDENT と同型** |
| `:bond_flavor` | ボンド 1 本ごとに個別 α × フレーバー | **何も課さない** | 空間対称性を一切仮定しない極端値。参照に対応物なし |

`:orbit` が押し付けていた本質的な制約は拡大セル並進ではなく
**`A^(1) = A^(2)`(Ψ が完全平方になること)** だった。

### パラメータ数の突き合わせ(ν=1/2, 32 サイト, 無向ボンド 192 本 = NN 64 + 2nd 64 + 3rd 64)

| | 参照 SUK_INDEPENDENT | `:orbit` | `:orbit_flavor` | `:bond_flavor` |
|---|---|---|---|---|
| 総数 | 92 | 24 | 48 | 384 |
| フレーバーあたり | **46**(ボンド 42 + オンサイト 4) | 24(共有) | **24** | **192** |

**`:orbit_flavor` は参照(46/フレーバー)より少ないのに参照と同じ −16.26 に到達している。**
足りなかったのは自由度の量ではなく**フレーバー独立性**。`:bond_flavor` は参照の 4.6 倍で、
「自由度をさらに増やすと単調に良くなるか」を見るための極端値にすぎない。

### 統計は壊れない

F が偶数(ボソン)でフレーバーごとに `A^(f)` を変えても、フレーバーロックにより
物理粒子を 1 組交換すると各行列式が (−1) を拾い、積は (−1)^F = +1。
DESIGN §1.3 の符号定理はそのまま成立する。独立化は統計を変えず自由度だけを増やす。

---

## 4. 棄却した仮説

### 4-1. 仮説 (A)「ゲージ方向が力ベクトルに残って更新を食っている」 → 棄却

v3.2 のゲージ射影とは別に、力ベクトル側でもゲージ方向を落とす実装を仮に入れて比較。
3 シードすべてでビット単位に近い一致(−10.887/−15.246/−8.917 vs
−10.888/−15.245/−8.922)。効果ゼロ。射影が既に効いているため。

### 4-2. `DSROptRedCut` → この範囲では無効、上げると有害

§5-3 の表のとおり、1e-8 と 1e-6 は**全シードで結果が完全一致**。閾値
`diag_max × RedCut` がどの S_ii も下回っておらず、**1 方向もカットされていない**。
1e-4 まで上げると seed 777 が −16.172 → −11.930 に悪化(参照の「1e-3 は過剰カット」と
同じ向き)。**現行の 1e-8 は安全側。**

---

## 5. 実験結果

条件はすべて sample=1500, warmup=500, dt=0.005, StaDel=0.01, RedCut=1e-8。
ν=1/2 は 2300 step、ν=1/3 は 5000 step(既存ベースラインと同条件)。

### 5-1. ν=1/2(ED −16.3049)、2300 step

| seed | `:orbit`(24 idx) | `:orbit_flavor`(48 idx) | `:bond_flavor`(384 idx) |
|---|---|---|---|
| 11272 | −10.887 | **−16.261** | −12.063 †(1911/2300 で打ち切り) |
| 4321 | −8.903 | −14.834 | 未実施(打ち切り) |
| 777 | **−16.172** | **−16.198** | 未実施(打ち切り) |

値は最終 100 step の平均(E_tail100)。

**`:orbit` → `:orbit_flavor` は 3 シードすべてで改善。** 最良 −16.2607 は ED の
0.27% 上で、参照実装の良好ラン −16.2610 と一致する。

`:bond_flavor` は途中打ち切りのため**結論を出せない**。1911 step 時点で −12.06 と
`:orbit_flavor` より悪いが、パラメータが 8 倍(48 → 384)なので収束が遅いだけの
可能性があり、この数字から「自由度を増やしすぎると悪い」とは言えない。
**そもそも参照実装(42 ボンドクラス/フレーバー)の 4.6 倍で、比較対象として不適切。**

### 5-2. ν=1/3(ED −10.1042)、5000 step

| seed | `:orbit`(42 idx) | `:orbit_flavor`(126 idx) |
|---|---|---|
| 11272 | **−9.967** | −8.795 |
| 4321 | −9.966 | −9.307 |
| 777 | **−9.987** | −9.108 †(3497/5000 で打ち切り) |

**ν=1/3 では逆にフレーバー独立化が悪化させている。** `:orbit` は 3 シードとも
−9.966〜−9.987 に収束(ED の 1.2% 上)して安定なのに、`:orbit_flavor` は 3 本とも
それより悪い。

打ち切った seed 777 は 3497 step 時点の値なので、収束が遅いだけの可能性は残る。
ただし完走した 2 本(11272 / 4321)も 5000 step で −8.79 / −9.31 に留まっており、
**「ステップ不足だから悪い」だけでは説明できない。**

† は打ち切ったラン。

### 5-2-b. この非対称は何を意味するか(未解決)

フレーバー独立化は **ν=1/2 を救い、ν=1/3 を悪化させる**。「常にフレーバー独立に
せよ」という結論にはならない。考えられる筋(いずれも未検証):

- ν=1/3 の目標状態は 3 つの行列式が同一である点(対称点)にあり、独立化で開いた
  方向はほぼ平坦。SR は平坦方向で MC ノイズを 1/ε 倍に増幅するので、
  ステップ数が同じなら**対称点から離れる方向に迷う**だけになる
- F=2(ボソン)と F=3(フェルミオン)で目標状態の対称性が違う
- ν=1/3 は `u_mf = 1.0` でオンサイト項があり、ν=1/2 には無い。オンサイト群の
  自由度が独立化でどう増えるかが違う

**ゲージ射影との相互作用も未検証**: `gauge_scale_groups` はフレーバーグラフの
連結成分なので、`:orbit`(共有)では全フレーバーが 1 群、独立化すると F 群に分かれる。
**射影の強さが idx 粒度によって変わる**ので、独立化の効果とゲージ射影の効果が
分離できていない。

### 5-3. 正則化因子(ν=1/2、`:orbit` 固定)

| 設定 | seed 11272 | 4321 | 777 |
|---|---|---|---|
| RedCut=1e-8, dt=0.005(基準) | −10.887 | −8.903 | **−16.172** |
| RedCut=1e-6, dt=0.005 | −10.887 | −8.903 | −16.172 |
| RedCut=1e-4, dt=0.005 | −10.905 | −8.866 | −11.930 |
| RedCut=1e-8, dt=0.01 | −10.900 | −13.818 | −11.961 |

RedCut は無効か有害。dt を 2 倍にすると盆地が入れ替わるだけで系統的な改善なし。
**全シードで一貫して改善したのはフレーバー独立化だけ。**

---

## 6. 計算コストの内訳(実測)

### 6-1. 1 ステップの内訳

ν=1/2、sample=1500、単独実行での平均(コンパイル分は除外):

| | 契約0/0′ | サンプリング | E_loc + O (`parton_main_cal!`) | SR 解 (`stochastic_opt!`) | 合計 |
|---|---|---|---|---|---|
| `:orbit`(2Npara=48) | 0.001 s | 0.009 s | **0.069 s** | **0.000 s** | 0.079 s |
| `:orbit_flavor`(96) | 0.001 s | 0.010 s | 0.051 s | 0.000 s | 0.062 s |
| `:bond_flavor`(768) | 0.009 s | 0.010 s | **1.221 s** | **0.004 s** | 1.245 s |

**SR ソルバは 768 パラメータでも 4 ms、全体の 0.3%。** 律速はすべて `parton_main_cal!`。

### 6-2. `parton_main_cal!` の内訳(サンプリングプロファイラ)

`:bond_flavor` の `parton_main_cal!` を 3 回プロファイルした結果、自己時間の最大は
`finalize_oo_store!`(`vmc_main_cal.jl:3645`)。`parton_local_energy` は 49 サンプルで
無視できる量だった。実体は素の三重ループ:

```julia
for i = 1:size_2, j = 1:size_2
    sum_val = 0.0 + 0.0im
    for s = 1:sample_size
        sum_val += O_store[i, s] * conj(O_store[j, s])
```

`size_2² × sample_size` = 1536² × 1500 ≈ 3.5×10⁹ 複素積。**S 行列の蓄積が律速。**

### 6-3. 既存 mVMC で使える削減手段

| 手段 | 状態 | 効果 |
|---|---|---|
| **MPI(サンプル並列)** | `weight_average_sr_opt!` / `reduce_counter!` は配線済み。**実装追加ゼロ** | O 蓄積も OO 蓄積もランク数で線形に割れる |
| **SR-CG(`NSRCG = 1`)** | `stochastic_opt_cg!` 系がフル実装済み。`_maincal_active_sr_opt_oo_length(sr; nsrcg)` が OO バッファを `size²` → `2*size` に落とす。`NStoreO` はパートン側 `main_cal` が既に読んでいる | **O(N_samp·N_para²) が消える** |
| スレッド並列 | `threading.jl` に per-thread の SROpt アキュムレータがあるが `parton_vmc_main_cal.jl` に利用箇所ゼロ(grep 0 件)。パートン経路はシングルスレッド | 未評価 |
| `finalize_oo_store!` の BLAS 化 | 既存関数の書き換えになるため**フォーク側では実施しない**。`OO = O_store * O_store'` そのものなので `mul!`/`syrk` で 1〜2 桁 | **upstream PR 候補** |

**SR-CG を開ける前に必須の再検証**: SR 力ベクトルの MF スロット共役シムは
NSRCG=0 前提で妥当性を確認したもので、`parton_vmc_main_cal.jl:314` に
「門番の `NSRCG = 0` がこのシムの妥当性を…」と明記してある。CG は S·v を O から
直接作るため共役を入れる境界が変わる。ここを詰めずに門番を開けると**静かに間違った
勾配**になる。現状の門番(`parton_unsupported_inputs.jl:186`)は正しい防御。

**優先度**: production 設定(`:orbit_flavor`、96 slots)では `main_cal` が 0.051 s/step で
そもそも問題になっていない。効くのは n_para が数百を超えてから。順番は
(1) MPI → (2) NSRCG=1(共役シムの再検証とセット) → (3) upstream へ BLAS 化提案。

---

## 7. 出力ファイル(CalcTime 系)の必要性 — 今回の実地の教訓

**この調査は、出力ファイル整備(v3.4)がなければ間違ったまま終わっていた。**

具体的に 2 回、出力ファイルが誤りを止めた/止められなかった:

1. **止めた例。** 私は `:bond_flavor` の速度を「ジョブ起動からの経過時間 ÷ ステップ数」で
   見積もり、**約 10 s/step = 6.4 時間/シード**と判断して実験を打ち切った。実際は
   `zvo_parton_time.dat` に **1.161 s/step**(= 45 分/シード)と記録されていた。
   **約 9 倍の過大見積もり**で、経過時間にはコンパイルと先行ランと CPU 競合が
   混ざっていた。ステップ単位の時間出力があったから訂正でき、実験を再開できた。
2. **止められなかった例。** 「どのフェーズが遅いか」は既存出力から一切分からず、
   アドホックにプロファイラ用スクリプトを書く必要があった。その結果
   「SR ソルバが重い」という私の推測が誤りで、実際は `finalize_oo_store!` だと判明した。
   **フェーズ別の時間出力があれば、この推測自体が生まれなかった。**

### 対応(v3.5 で実装済み)

- **CalcTimer をパートンモードで既定 ON** にした(既存モードは `MVMC_C_TIMER` の
  opt-in のままで既定を変えていない)。新しいタイマ機構は作らず、既存 `c_timer.jl` の
  枠組みで `zvo_CalcTimer.dat` にパートンセクションを追記する。
  **ID 帯は 800–813**(使用中は 0–72 / 600–603 / 920–966。C 版の体系からも
  Julia 移植の診断からも離れた空き帯)。区間は 契約0 / 0′ / サンプリング骨格 /
  契約1・2・3 / main_cal / 契約4・5・**OO 蓄積** / SR / 同期 / 出力。
  §6-2 で判明した律速(OO 蓄積)が今後は出力ファイルから直接読める。
- v3.4 で「`MVMC_C_TIMER` は既定 OFF のまま」とした判断は**撤回**した。当時の理由は
  「step 単位の wall-clock で日常の診断には足りる」だったが、今回まさに足りなかった。
- 見積もりの根拠に**経過時間の割り算を使わない**。`zvo_parton_time.dat` を読む。

---

## 8. 未解決・レビューで見てほしい点

1. **`:orbit` でも seed 777 は −16.172 に到達している。** つまり 24 idx の多様体は
   −16.2 の状態を含む。表現力の問題ではなく**到達できる盆地が極端に狭くなる**という
   最適化ランドスケープの問題。なぜフレーバー共有だと盆地が縮むのかは説明できていない。
2. **ν=1/2 と ν=1/3 の非対称。** `:orbit` のままでも ν=1/3 は 3 シードすべてが
   −9.96〜−9.99 に収束し、ν=1/2 だけが 1/3 しか届かない。フィクスチャの差
   (拡大セル (2,1) vs (1,3)、u_mf の有無)か、F=2 と F=3 の差か、未特定。
3. **`bond_class` の縮約の妥当性。** 両端点の鍵の `min` を代表に採る実装(軌道割れ防止)。
   この縮約が π-flux 構造を表現できる自由度を残しているかは、間接的に
   (seed 777 が −16.17 に届くこと)しか確認していない。
4. **ゲージ射影とスケール群の解像度。** `gauge_scale_groups` はフレーバーグラフの
   連結成分なので、`:orbit`(共有)では全フレーバーが 1 群、`:orbit_flavor` では F 群に
   分かれる。**射影の強さが idx 粒度によって変わる**。これが §8-1 の盆地の広さに
   効いている可能性があるが未検証。
5. **ν=1/2 と ν=1/3 でフレーバー独立化の効果が逆転する理由**(§5-2-b)。今回いちばん
   大きな未解決点。ここが説明できるまで「P 層の既定を `:orbit_flavor` にする」とは
   決められない。
6. **フレーバー独立化とゲージ射影の効果が分離できていない。** `gauge_scale_groups` は
   フレーバーグラフの連結成分なので、idx 粒度を変えると射影の強さも同時に変わる。
   `PartonGaugeFix = 0` との 2×2 を取れば分離できるはずだが未実施。
7. **P4 ベンチマークの指標。** 参照は Adam なら安定して −16.26 に到達している。
   「SR での到達値」を報告すると最適化器の性質を測ってしまう。アンザッツの表現力を
   測りたいなら別の指標(複数シードの最良値など)を明示すべき。

---

## 9. 再現手順

```
# 候補 C(idx 粒度) ν=1/2 / ν=1/3
julia --project=MVMCOptimizers.jl <job>/tmp/run_candC.jl      # → playground_nozomi/parton_candC/
julia --project=MVMCOptimizers.jl <job>/tmp/run_candC13.jl    # → playground_nozomi/parton_candC13/

# 正則化因子(RedCut, dt)
julia --project=MVMCOptimizers.jl <job>/tmp/run_regD.jl       # → playground_nozomi/parton_regD/

# 1 ステップの内訳 / main_cal のプロファイル
julia --project=MVMCOptimizers.jl <job>/tmp/prof.jl
julia --project=MVMCOptimizers.jl <job>/tmp/prof2.jl

# 既存の P3 複数シード
#   → playground_nozomi/parton_p3/{nu12,nu13}_*_out/zvo_out.dat
```

---

## 10. 追記: M2(運動量射影)着手時に見つかった 2 つの実バグ

**どちらも n_qp = 1 では表に出ず、M1 の全緑をすり抜けていた。** 「M1 で緑だったから
正しい」が成立しない実例なので、レビュー時の参考に残す。

4 サイト・並進 2 種・重み `1  0.0  1.0`(= 純虚数 i)の `qptransidx.def` を実際に
読ませた結果(修正前):

```
qp_trans      = [[0, 1, 2, 3], [1, 2, 3, 0]]     ← 値が 0-based
para_qp_trans = ComplexF64[1.0+0.0im, 0.0+0.0im]  ← 虚部が落ちている
```

### (1) qp 写像の base 不一致

upstream は `qp_trans` の値を 0-based で持つ(`slater_update.jl:642` が
`xqp[ri+1]` の結果をそのまま 0-based サイトとして使う)。一方パートンの
契約 1/2/3/5 は `Φ[qmap[r], :]` と、返り値を **Φ の行番号(1-based)** として使う。

`parton_ensure_qp!` の恒等フォールバックが `collect(1:n_site)` と 1-based を
入れていたため n_qp = 1 では偶然一致し、実 def を与えると全行が 1 ずれる
(site 0 に写る行は `Φ[0,:]` で BoundsError)。

修正: DESIGN §3.1 規則 8「0→1 変換は一箇所」を qp 写像にも適用し、
`parton_normalize_qp_trans!` で入口 1 回だけ正規化(冪等・全単射検査つき)。

### (2) ParaQPTrans の虚部が読まれていない

運動量射影の重みは `exp(2πi k·R)`。参照実装 `build_def.jl:646` は
`(Opidx, real(amp), imag(amp))` の 3 列で書き出すが、読み手は第 2 トークンしか
見ていなかった。**k ≠ 0 で位相が丸ごと落ち、エラーにならず重み 0 の項として
静かに消える。** 3 列目があれば虚部として読むよう修正(2 列の既存ファイルは
虚部 0 で従来どおり = 後方互換)。共有パーサへの修正なので登録点マーカーつき。
**upstream への報告候補。**

### まだ検証していないこと(次セッションの起点)

- ~~**gather の向き**~~ → **§11 で決着**(実装は正しかった。符号規約を新たに確定)
- ~~門番の不整合検査~~ → **§12-1 で実装**(`validate_parton_qp`)
- ~~CB 模型の QP 構成~~ → **§12-2 で決着**(構成が 2 系統あった。実運用は
  `make_QNPidx` = x 方向 kext 本)
- エネルギー低下の検証 → **§12-4**(実験中/結果を参照)

---

## 11. gather の向きの決定論的検証(決着)

### 結論

**実装の向きは正しい。プロダクションコードの変更はなし。** 検証は恒久テスト
`MVMCOptimizers.jl/test/test_parton_qp_momentum.jl`(DESIGN §8-11、35 assert)。

副産物として**符号規約が 1 つ確定した** — def の重み `exp(2πi K·R)` に対し

    T_S|ψ⟩ = e^{+2πi K·S}|ψ⟩        (実装が与える並進固有値)
    T_S|ψ_k⟩ = e^{-i k·S}|ψ_k⟩       (標準の Bloch 規約)
    ⇒ k = −2πK

**def の K は標準規約の運動量の符号を反転したラベル**。C-mVMC の
`tri = xqp[ori+1]` 規約をそのまま引き継いだ結果なので参照実装との比較では整合し、
バグではない。ただし**時間反転が破れた系(複素ホッピング・フラックス)では K と −K は
別の状態**なので、狙った運動量セクターを指定するときは意識が要る。パートン模型は
まさにそういう系なので、CB 模型の QP 構成に進む前にここを踏んでおく価値があった。

### 向きが「正しい」ことの根拠(2 系統)

**(a) 静的な突き合わせ** — 参照実装の def 生成と Julia 側パーサが同じ列順を指している:

| 層 | 場所 | 内容 |
|---|---|---|
| def 生成 | `VMC/src/Model/CheckerBoard.jl:264` `build_QNPTransSiteList` | `isite = jsite + R`、`push!(..., (isite, jsite, Opidx))` |
| def 生成 | `VMC/src/buildDefFile/build_def.jl:678` `build_TranslationalOperatorparams` | データ行 `(Opidx, jsite, isite, 1)` = **col2 が元サイト、col3 が並進後** |
| パーサ | `orbital_qptrans_utils.jl:264` `build_qp_trans_mappings!` | `qp_trans[mpidx][j] = itmp` (j=col2, itmp=col3) → **順写像** |
| gather | `parton_calculate_m_all.jl:31` | `Φ[qmap[r_m], n]` — upstream `slater_update.jl:642` の `tri = xqp[ori+1]` と同じ形 |

**(b) 動的な機械検証** — 実装式を参照しない参照値で 2 つの独立経路から:

1. **配置側の並進から**。射影 `P = Σ_R w_R T_R` は群の要素の線形結合なので、
   `w_R = e^{2πinR/L}` なら |φ⟩ が何であっても `T_S P|φ⟩ = w_S^{±1} P|φ⟩`。
   配置の並進 `τ_S: r ↦ r+S` で書くと `ip(τ_S x)/ip(x) = e^{∓2πinS/L}` で、
   **指数の符号が写像の向きそのもの**。参照値は `n, S, L` だけで決まる。
   L=6, n=1, F=2, Ne=3、並進不変**でない**平均場、3 配置 × 全 S で `e^{-2πinS/L}` に一致。
2. **Φ のバンド運動量から**(1 の τ_S の向きに依存しない裏取り)。平均場を一様にすると
   |φ⟩ 自身が並進固有状態になるので、射影が非ゼロで生き残る K が一意に決まり、
   その K は占有軌道の運動量和から qp 経路と無関係に予言できる。
   予言 `n* = 4`(K_0 = 2π/3)に対し、実装も n=4 でだけ非ゼロ・他の 5 通りは 1e-10 未満。

### 方法論の教訓(レビュー時の参考)

**§8-3 が向きについて検出力ゼロだったのは、参照側の「実体化版」を
`Φqp[r,n] = sgn[r]·Φ[shifts[qp][r], n]` と実装とまったく同じ式で定義していたから。**
向きを取り違えると両辺が同時にずれる。n_qp を増やしても符号 sgn を凝っても上がらない。
これが M1 全緑をすり抜けた理由で、§10 の 2 バグと同じ構図(「緑だったから正しい」が
成立しない)。

対策として **検出力の証明をテスト自身に同居させた**:

- 写像を逆向きにしたミューテーションで固有値が複素共役に振れることを assert
- `n* ≠ −n*`(K がゼロでもゾーン境界でもない)を assert — パラメータを変えて
  K_0 = 0 や π を選ぶと検出力が消えるので、そのときはテストが落ちて気づける

参照値が実装から独立でも、**ミューテーションで落ちるのを見ていなければ
「検出できるテスト」とは言えない**。

### 再現

```
julia --project=MVMCOptimizers.jl MVMCOptimizers.jl/test/runtests.jl
#   → Parton Mode Tests 962 pass(追加前 927、+35)、Unit Tests 793 pass
```

---

## 12. QP の門番と CB 模型の QP 構成

### 12-1. 門番: QP の本数整合(`validate_parton_qp`)

`NMPTrans` は modpara.def、`qp_trans` / `qp_trans_sgn` / `para_qp_trans` は
qptransidx.def と**別経路**で入る。振幅側は `get_n_qp_full`
(= NSPGaussLeg × NMPTrans × NQPOptTrans)で寸法を決め、gather は
`data.qp_trans[qp]` を引くので、片方だけ書き忘れると:

| 食い違い | 現行の挙動 |
|---|---|
| 写像が足りない | `qp_trans[qp]` で BoundsError(まだ気づける) |
| 写像が余る | 後ろの並進が**黙って無視**される |
| `para_qp_trans` が短い | `init_qp_weight!` が 0 のまま残す → **重み 0 の項**として消える |

加えて **`NMPTrans < 0`(C の APFlag = 反周期境界)を拒否**する。
`init_qp_weight!` は `abs(NMPTrans)` で N 本の重みを作るのに `get_n_qp_full` は
`max(1, NMPTrans)` で **1 に潰す**ので、射影が黙って恒等 1 本に縮退する。
扱いが食い違っている既存の穴で、パートン側は符号を `qp_trans_sgn` で運べるため
この経路を使う必要がない。テスト 12 件(`test_parton_gatekeeper.jl`)。

### 12-2. QP 構成が 2 系統あった(今回いちばん重要な発見)

参照実装には QP を作る関数が **2 つ**あり、パートン v2 が使うのは後者:

| 関数 | 本数 | 並進 |
|---|---|---|
| `build_QNPTransSiteList` (`CheckerBoard.jl:264`) | 全並進 `Nu = Nsite/2` | x, y 両方 |
| **`make_QNPidx` (`CheckerBoard.jl:280`)** | **`kext`(= puc = K)** | **x 方向のみ** |

`_make_qnp_mod_parton` の `:v2` 経路が `make_QNPidx` を呼ぶ。参照実装をそのまま
実行して確認した対応(Nux=Nuy=4, Nsite=32):

```
K(puc)=1 -> NQPTrans=1   uclist=[(0,0)]
K(puc)=2 -> NQPTrans=2   uclist=[(0,0),(1,0)]
K(puc)=4 -> NQPTrans=4   uclist=[(0,0),(1,0),(2,0),(3,0)]
```

**なぜ x 方向 kext 本で足りるのか**: アンザッツは x 方向に kext セルの拡大周期を
持つので、破られているのは剰余群 `Z_kext` の分だけ。`T_x^kext` と y 方向の並進は
**保たれている**ので、それらで射影しても |φ⟩ は(規格化を除いて)変わらない。
得が無くコストだけ増えるから入れない、という**実務的な**理由。

**原理的には全並進でも下がる(前稿の訂正)**: 当初ここに「保たれている対称性まで
射影すると変分空間を余計に狭めるので悪化する」と書いたが、**これは誤り**。
§12-3 の 5 で確かめたとおり、どの部分群で射影しても
`Σ_k w_k E_k = E_noproj`(凸結合)は成り立つので `min_k E_k ≤ E_noproj` は常に成立し、
全並進が原理的に不利ということはない。全並進 16 本の k=(0,0) が悪化して見えたのは
**800 step で未収束だっただけ**で、同条件を 2300 step に伸ばすと改善に転じた
(§12-4)。**短い run の途中経過から機構を推論してはいけない**という、§8-3 や
§10 と同じ型の誤り。

**Kx / Ky の意味**: kext=2 のとき重みは `exp(2πi·nkx·ucx/2)` なので
`nkx=0 → Kx=0`、`nkx=1 → Kx=π`(基本セル並進あたりの位相 = 重み (1, −1))。
**Ky は射影していない**(QP に y 方向並進が無いのでセクターは混ざったまま)。

### 12-3. 恒久テスト(P 層 P2、`test/physics/test_p2_qp_translation.jl`)

射影が変分的に正当なのは `T_R` が **H_phys の対称性**のときで、H_MF の並進
不変性は不要(むしろ破れているから射影に意味がある)。この区別に沿って検査する:

1. 並進写像が巡回群として閉じた全単射
2. **cb 模型の 1 体項が全並進で不変**(= 射影が変分的に正当)
3. 拡大セルで縮約した平均場は基本セル並進で**不変でない**(= 射影が自明でない)
4. `qptransidx.def` が往復し、門番を通る
5. **射影の変分的性質**(全数展開・機械精度):
   `Σ_k w_k E_k = E_noproj`(凸結合)と `min_k E_k ≤ E_noproj`
6. 参照実装 `make_QNPidx` と同じ本数・同じ並進
7. 参照準拠 QP でも 5 が成り立つ

5 は**配線の総合ゲート**になる — 写像の向き・重みの位相・n_qp の寸法が同時に
正しくないと成立しない。P 層 421 pass。

### 12-4. エネルギー低下の検証と「QP にバグがあるのでは」の切り分け

条件は §5 と同じ(sample=1500, warmup=500, dt=0.005, StaDel=0.01, RedCut=1e-8,
2300 step)。QP は参照準拠の kext=2(kx0 = Kx=0、kx1 = Kx=π。**Ky は射影して
いない** — QP に y 方向並進が無いのでセクターは混ざったまま)。値は E_tail100。

**`:orbit`(ランダム初期値):**

| seed | noproj | kx0 (Kx=0) | kx1 (Kx=π) |
|---|---|---|---|
| 11272 | −10.887 | **−13.114** | −11.323 |
| 777 | −16.172 | **−16.214** | −12.068 |

射影が**両シードで改善**し、**Kx=0 が最良** — 「K=(0,0) が基底状態セクター」
(支給事実)と整合。とくに局所解に嵌っていた seed 11272 が −10.9 → −13.1 と
大きく動く。

**`:orbit_flavor`(ランダム初期値)では逆転する:**

| seed | noproj | kx0 | kx1 |
|---|---|---|---|
| 11272 | **−16.259** | −14.737 | −15.514 |
| 777 | **−16.200** | −14.943 | −15.412 |

noproj が既に ED(−16.3049)の 0.3% 上に達するのに、射影 ON は 4 本とも悪い。
これが「QP の実装に問題があるのでは」という疑いの出所になった。

ただし E_best(run 中の最良値)を見ると **kx0_seed777 は −16.2695 まで一度
到達してから漂って離れている**(E_tail100 は −14.94)。盆地に入れないのでは
なく、**射影 ON だと最適点近傍での滞在が不安定**という描像。下の検証 3
(ウォームスタートでは 500 step 留まる)と合わせると、恒常的な故障ではなく
確率的な不安定性で、SR ノイズ起源の筋(下記)と整合する。

**切り分け(4 つの独立検証、すべて実装は正しい):**

1. **固定 α 評価**: `:orbit_flavor` noproj の最適化済み α(−16.26)を
   `InPmfPara.def` で読み戻し、dt=0 で MC 評価だけを行う。
   noproj −16.2603 / kx0 **−16.2578**(一致) / kx1 −15.101。
   最適化済み状態はほぼ純粋な Kx=0 セクターで、**射影つき評価は正しい**。
   kx1 成分が高エネルギー・小重みという構造も基底セクター Kx=0 と整合
2. **力 = 有限差分勾配 @ n_qp > 1**(§8-7 の拡張、恒久テスト): 契約 5 の
   「∂Φ も QP 写像を通す」経路は n_qp = 1 では恒等になり未検証だった。
   6 サイト環・Z_6 全並進・k=1 複素重みの全数展開で、力ベクトルと有限差分勾配が
   機械精度一致(store / 非 store 両経路)。**勾配も正しい**。
   なお最初 4 サイトで書いたら k=1 セクターが 1 次元しかなく勾配が恒等的に
   ゼロになってテストが退化した(テストサイズの罠として §8-7 に記載)
3. **ウォームスタート**: 最適化済み α から**射影 ON のまま** SR を 500 step 継続
   → E_tail = −16.2616 に**留まる**(対照 noproj −16.2600)。
   **射影つき SR ループは能動的に悪いことをしていない**
4. `:orbit` では射影が両シードで改善し、セクター順序も物理と整合(上表)

**結論: QP 実装(評価・勾配・SR ループ)にバグは見つからない。**
`:orbit_flavor` + 射影 + ランダム初期値の劣化は**最適化ランドスケープの問題**。
この系 × SR が多谷・初期値敏感なのは §2-1(参照実装でも −7.3 に落ちる)や
§5(seed で −10.9 / −16.2 に分かれる)で既知で、その同類。機構の候補
(いずれも未検証):

- ランダム初期値の |φ⟩ は重みが全セクターに散っており、射影後の |ip|² は
  ノードが増えてサンプリングが荒れる。SR 初期の force が noisy になり、
  変な盆地に押し込まれる
- 射影は α の実効自由度を減らす(P|φ⟩ が同じ状態になる α の同値類が太る)ので、
  S 行列の平坦方向が増え、初期段階の SR が迷いやすい

**実務上の含意**: 射影は「良い状態に到達してから ON にする」(ウォームスタート)
のが安全。ランダム初期値から射影 ON で回すなら、`:orbit` のように射影が盆地を
選別してくれる場合もあるが、保証はない。

---

## 13. M2 後半: 物理密度 Jastrow(v3.11)

### 定義と上流との関係

    P_J(x) = exp( Σ_{i<j} v_{ij} n^b_i n^b_j ),  n^b ∈ {0,1}

上流 `make_proj_cnt!` は x = n − 1 で数えるが、パートン和 F·n^b − 1 を使うと
v の意味が F 依存になるため n^b で定義(構造の規約 — Σ_{i<j}・自己項なし・
対称 idx・exp の符号 + — は上流と同一)。差は一体項+定数で、係数の辻褄合わせは
入れていない(DESIGN §2.3.2)。

### 実装の要点

- **P_J は qp に依存しない**ので、比・E_loc の式は既存の exp(log_pr) フックのまま。
  比は純粋関数 O(Ne)(カウンタ不要)、カウンタは契約 5(O_p = cnt_p、実数)と
  サンプル保存だけが読む
- 受理判定は上流と同じ「log をまとめて 1 回 exp + !isfinite → 棄却」。
  **Jastrow なし(log_pr = 0)は従来の式そのまま** — v3.10 出力と
  全ファイルバイト一致を実測で確認済み(zvo_out / pmfpara_opt / var / SRinfo)
- Gutzwiller は固縛下でダブロン数 = NElec の定数になり自明化するため、
  門番が理由つきで拒否(DH2/DH4 も同様)
- 共役シムの前提「MF 以外の O は実数」は、v 実数(門番)+ Im 凍結(flags)+
  虚スロット厳密 0(fill)の 3 点で配線的に保証。§8-16-4 が機械検証
- 既存経路の再利用: jastrowidx.def / InJastrow.def のパーサ、SR 書き戻し、
  MPI 同期、zqp_jastrow_opt.dat — **新しい登録点はゼロ**

### 検証(§8-16、全緑)

差分 vs 全数(自己項ガード)/ ∂E/∂v の有限差分 = 力ベクトル / S が共役の有無で
不変 / v=0 からの厳密勾配ステップで E が厳密に下がる / n_qp=2 併用 / F=2,3。
全体回帰 Parton 2535 / Unit 793。

### 最終検証(CB 模型・transJastrow・拡大セル (2,0)・Kx=Ky=0・nqp=2・NVMCSample=2000)

transJastrow = 無順序ペアを**全基本セル並進**の軌道で類別(参照実装
`symmetrize_jastrow_idx` の trans_maps = 全並進と同じ同値類。P_J が全並進と
可換でないと、射影の外側にかかる P_J が運動量固有性を壊す)。34 クラス。

**コールドスタート(ランダム α、2300 step)**:

| seed | Jastrow なし | Jastrow あり(同時最適化) |
|---|---|---|
| 11272 | **−16.2554** | −14.7493 |
| 777 | **−16.2322** | −15.5852(E_best は −16.2603) |

同時最適化は §12-4 / §14 のランドスケープ・SR 安定性問題に嵌り、比較にならない。

**ウォームスタート(nojast 最適 α + v = 0 開始、800 step)** — v = 0 は Jastrow
なしと同じ状態なので、ここからの降下は変分的に E を下げる方向にしか動けない:

| run | E_tail100 | E_best | gap(末尾) | diagCut |
|---|---|---|---|---|
| **warmjast** | **−16.2636** | **−16.2860** | 0.50 | 0 |
| warmnojast(対照) | −16.2572 | −16.2792 | 0.39 | 0 |
| ベースライン(2300 step) | −16.2554 | −16.2760 | 0.29 | 0 |

**Jastrow ON が対照とベースラインの両方を下回った**(Δ ≈ −6.4 mE vs 対照。
ED −16.3049 とのギャップを −16.2554 比で約 17% 詰めた)。gap 健全・diagCut = 0 の
まま安定なプラトーに乗る — 既存 mVMC と同じ挙動。**M2 の受け入れ条件
(エネルギーが既存を下回る)はウォームスタート・プロトコルで達成**。

**応急処置(§14)込みのコールドスタート再走(red_cut 1e-6)**: seed 777 は
**E_tail100 = −16.2545**(E_best −16.2695)に定着し、nojast の −16.2322 を
コールドスタートでも下回った(red_cut 1e-8 では −15.59 へ漂流していた run)。
seed 11272 は −14.04 のままスタック — こちらは §12-4 のランドスケープ問題で、
応急処置の守備範囲外。**2 シード中 1 つをコールドスタートで救済、残りは根治待ち**。

---

## 14. SR が「落ち切らない」機構の特定(gap 崩壊 × RedCut 大量カット)

ユーザー指摘のとおり、「良い値に触れてから漂う」は Jastrow 固有ではない
(v3.8 の nojast kx0 でも発生)。全 run の diag / SRinfo を突き合わせて機構を特定した。

### 証拠の連鎖

1. **不安定 run は例外なく min_gap が潰れている**(1e-7〜1e-8)。唯一の完全成功
   nojast_seed11272 は step 1200 で gap が 0.28 に回復し、以後 sDiagMax が
   1e7 → 3.06 に落ちて安定化
2. **sDiagMax ∝ 1/gap²**(実測: gap²·sDiagMax ≈ 1e-2〜1e-1 で一定オーダー)。
   契約 0′ の ∂Φ = U(W./D) の分母 D = ε_n − ε_u が gap で下から抑えられる
   **だけ**(DESIGN §1.5 の「min_gap が保険」は @warn のみ)なので、
   O_MF ∝ 1/gap → S 対角 ∝ 1/gap²。jast_seed777 終盤で sDiagMax = 1.1e+14
3. **RedCut 閾値 = sDiagMax × 1e-8 が跳ね上がり、健全な方向を大量カット**:
   jast_seed777 は E_best 到達期(diagCut = 0)→ 漂流期(diagCut = 82/130、
   Jastrow の v 全 34 方向を含む)。nojast_seed777 も 0 ↔ 48 を往復
4. カットされなかった残りは 1/gap の特異方向 → SR はそこしか動けず、
   プラトーに定着できない

### 含意

- §5-3 の「RedCut = 1e-8 は安全側(1 方向もカットされない)」は **gap 健全域限定**。
  gap 崩壊域では相対閾値が逆に牙をむく
- 既存 mVMC で起きないのは f_ij に 1/gap 増幅構造が無いから。**パートン固有**
  (α → eigen → Φ の連鎖に固有)
- Jastrow は原因ではなく**被害者**(v 方向がカットされる側)。パラメータ増で
  発症確率は上がる

### 対処の選択肢(未実施 — 数式・意味論に触るので判断待ち)

### 参照 chi-VMC の対処(調査結果)

参照も**同じ現象に遭遇している**: 成功 run(o10524、最終 −16.261)で
diag_max = 9.9e+11、n_cut = 100/184 が 76 回、E も途中で上下しつつ 2200 iter で
到達。対処は (1) 摂動分母の clamp(`vmc_chi_grad.jl:77`、|Δλ| < 1e-8 → 0)、
(2) SR 解の NaN/Inf ゲート(本 iter スキップ)、(3) red_cut = 1e-6
(「1e-3 は過剰、1e-6 が必須」)、(4) あとは乗り切る。根治はしていない。

### 「波動関数がゼロを踏む」説との関係(分析)

古典的なゼロ踏み(配置空間のノード)は**統計現象**: サンプルがノード近傍に
落ちると O ∝ 1/Ψ が跳ね、1 サンプルが sMax を支配する。サンプル増で緩和し、
波動関数が育つと消える(最初の数百 step で収まる、という経験則の正体)。

今回は署名がすべて逆で**別物**:

| 署名 | ゼロ踏みなら | 実測 |
|---|---|---|
| E_loc 分散のスパイク | 断続的に数桁 | 外れ値比 ~1.0、全期間平坦 |
| ratio_floor ヒット | 発火 | 0 回 |
| sMax | step ごとに暴れる | 1/gap² に決定論的に追従 |
| サンプル数依存 | 増やせば緩和 | 無関係 |

実体は**パラメータ空間の準ノード**(フェルミ準位の level crossing で α→Φ が
微分不能に近づき、**全配置で一様に** ∂Φ ∝ 1/gap)。症状(sMax 発散・diagCut
膨張・E 上昇)がゼロ踏みと同型なので同じ話に見えるが、統計ではないので
「このサンプル数なら安定するはず」が通用しない。

### 対処(v3.11 で実施済み = 応急処置)

| 案 | 内容 | 状態 |
|---|---|---|
| 参照互換 clamp | 契約 0′ で \|ε_n−ε_u\| < 1e-8 の摂動項を 0 | **実装済み**(§8-17-1) |
| NaN/Inf ゲート | 非有限な更新の iter を巻き戻してスキップ | **実装済み**(§8-17-2) |
| RedCut 1e-6 | 参照の「必須」値。fixture 既定を変更 | **実施済み**(コールドスタート seed 777 を救済: −15.59 漂流 → −16.2545 定着。11272 は盆地問題で対象外) |
| ウォームスタート運用 | nojast → Jastrow ON。数式不変 | **実証済み**(§13) |
| gap トラスト領域 / sr_eig_cut | ステップ制御・固有値カット | **未実施(根治候補、DESIGN §10)** |

---

## 15. SR 漂流の機構確定 — 遡及診断と単ステップ再生(2026-08-14)

**§14 の機構同定は主犯を取り違えていた。** 追加サンプリングなしの遡及診断と、
同一サンプル上でのソルバ差し替えにより、以下が確定した。

- **主犯は「アウフバウ占有規則による枝の不連続な乗り換え」**。gap が潰れた領域では
  Fubini–Study 計量で見て**通常サイズの 1 ステップ**でも準位の順序が入れ替わり、
  占有スライスが不連続に飛ぶ(実測: 主角 89.99°、同じ step で E が +1.1)
- **RedCut の大量カットは主犯ではなく症状**。発症後に α を凍結させて回復を妨げる
  共犯ではある(「漂流」の実体は**凍結**だった)
- **条件数説は棄却**、**セクター説はこの ansatz には適用不可**

診断コードは `playground_nozomi/diag_sr/`(本体パッケージ非依存、読み出しのみ)。

| スクリプト | 役割 |
|---|---|
| `probe_structure.jl` | 入力・出力ファイルの構造検証(α 履歴の復元が opt と一致することの確認) |
| `replay_trajectory.jl` | 全 SR ステップの H_MF を再構築して c_θ / ‖∂Φ‖² / 主角 / ゲージ方向を再計算 |
| `onestep_replay.jl` | 1 点で O と E_loc を生成保存し、4 案のソルバを同一データに適用 + 相関サンプリング |
| `multistep_replay.jl` | 発症点から N ステップだけソルバを差し替えて回す縮小実験(MOM 版を含む) |
| `sector_analysis.jl` | 並進対称性の実測と Chern 数の可否判定 |
| `analyze_step0.py` | 上記の集計 |

### 15-1. §1-a  c_θ とカット集合の突き合わせ(機構同定の直接検証)

`c_θ = ⟨u|∂_θ H_MF|n⟩` は MC 不要・決定論的に計算できる。**剛方向**(2 準位部分空間を
回す非対角 `⟨u|∂H|n⟩`)と **gap 開閉方向**(対角差 `⟨u|∂H|u⟩ − ⟨n|∂H|n⟩`)は区別した。

- `‖∂Φ_a‖²`(決定論的)と `sDiagMax`(MC 実測)の比 = **0.45〜2.2**。14 桁の変動を通じて
  追従する(log10 比の中央値 −0.02〜−0.38)。**S の対角最大は統計ではなく ∂Φ の
  決定論的増幅で決まる**
- `dphi2_max = (rigid_max / gap)²` が数値的に成立。jast_seed777 step 2299 で
  `(0.4169 / 3.696e-8)² = 1.27e14` = 実測 `dphi2_max 1.272e14`
- **カット集合と `|c_θ|` は対応する**。step 2299 で生存 47 本の `rigid` 中央値
  3.3e-2、カット 49 本は 5.1e-5 — **3 桁の差**。対角方向(gap 開閉)の最大は 0.72 で
  剛方向とは別集合
- 予測カット数と実測 `diagCut` の突き合わせ: jast_seed777 の `sDiagMax > 1e9` の
  **377 step すべて**で「MF 予測 + Jastrow 34 本 = 実測 diagCut」が一致。単ステップ
  再生でも「現行 RedCut が切る本数 82/130」= 実測 `diagCut 82`

**機構同定そのものは正しい。ただしこれは gap 崩壊の帰結であって原因ではない**(15-4)。

### 15-2. §1-b  S_aa の分布と死床の設計

発症点(jast_seed777 step 2299、gap 3.7e-8)の `S_aa` は**完全な二峰性**:

```
  10^-2 .. 10^-1 :  23      10^+7 .. 10^+8  :   1
  10^-1 .. 10^+0 :  38      10^+9 .. 10^+10 :   2
  10^+0 .. 10^+1 :  21      10^+10.. 10^+11 :  11
                            10^+11.. 10^+12 :  10
                            10^+12.. 10^+13 :  16
                            10^+13.. 10^+14 :   6
                            10^+14.. 10^+15 :   2
```

中間が空いている。健全側 82 本(Jastrow 34 + MF 48)と発散側 48 本。

**死方向は座標軸上に 1 本も無い**:

- `S_aa < 2·SE(S_aa)` で切れる本数 = **0**。`S_aa / SE` の最小は 6.97
- `S_aa < 1e-6 · median` でも **0**
- ゲージ平坦方向(スケール群)の `‖∂Φ‖²` は 1e-18〜1e-29 = 機械精度ゼロだが、
  **座標軸には乗らない**(群全体を実数倍する方向)ので対角には現れない。
  独立なスケール群は 2 個で DESIGN §2.5 の連結成分(フレーバー 2・共有なし)と整合

含意: **対角ベースの死床は「効かない」のではなく「切るものが無い」**。同時に、
対角スケーリング `D^{-1/2}` が死方向を増幅するという懸念(外部レビュー §0-b-5)も
この系では**該当しない**。床は保険として残すが、発火しない。

`SE(S_aa)` の入手経路: **既定の store 経路(`NStoreO≠0`)はサンプル毎 O を
`sr_opt_o_store` に保持しているので追加の累積器は不要**。非 store 経路のみ
`Σ_s w_s |O_a|⁴` の累積器が 1 本要る。

### 15-3. §1-c / §1-d  枝の連続性とセクター

- 占有部分空間の主角の中央値は 0.05〜0.24°(連続)。ただし**主角 ≈ 90° の
  占有反転が離散的に発生**する: jast_seed777 で 2 回、jast_seed11272 で 5 回、
  nojast_seed777 で 1 回、nojast_seed11272 で 3 回。**すべて gap ≤ 1e-4 の状態**
- 反転と E ジャンプは**同一 step で同期**する:

  | run | step | E(前 → 当 → 次) | gap |
  |---|---|---|---|
  | jast_seed777 | 509 | −16.2282 → **−14.8781** → −14.9252 | 7.7e-6 |
  | jast_seed777 | 1774 | −16.2376 → **−15.1472** → −15.1555 | 3.4e-6 |
  | nojast_seed777 | 469 | −16.2242 → **−14.9584** → −14.9879 | 8.3e-6 |

- **セクター(Chern 数)はこの ansatz では定義できない**: `H_MF(α)` は恒等以外の
  並進で不変でない(最適化後も相対 1.5e-3 のずれが残る)ので Bloch 化ができない。
  加えて充填が 8/32 = 1 セルあたり 0.5 粒子なので、仮に対称でも占有バンドが
  整数本にならない。**「seed 11272 は別トポロジカルセクターの局所極小」という
  仮説は検証以前に適用外**(実際の原因は 15-4)

### 15-4. §1-e  非アウフバウ最適解 — これが主犯

`corr(E, log10 gap)` が**失敗ケースだけ正**になる:

| run | pearson | E 下位 2% の gap 中央値 | E 上位 2% |
|---|---|---|---|
| jast_seed777(漂流) | **+0.236** | 3.0e-6 | 2.6e-1 |
| jast_seed11272(スタック) | **+0.429** | 1.5e-3 | 3.2e-2 |
| nojast_seed777 | **+0.316** | 4.1e-7 | 2.6e-1 |
| nojast_seed11272(**唯一の完全成功**) | **−0.620** | **2.7e-1** | 3.2e-2 |

「E を下げると gap が閉じる」= 最適解が「下から Ne 個」以外の占有を要求している
兆候。**MOM(前ステップの占有部分空間との重なり最大で Ne 本を選ぶ)を入れた縮小実験で
確定した**:

E_best 近傍(jast_seed777 step 1570、E = −16.2441)から 400 step:

| ソルバ | E 末尾 50 平均 | gap 末尾 | RedCut 発火 |
|---|---|---|---|
| 現行(RedCut max 相対) | −15.3462 | 3.46e-06 | 186/400 step |
| RedCut 撤廃(案 1) | −15.3411 | 3.33e-06 | 0 |
| 案 4(モード別 S/N) | −15.2884 | 1.32e-07 | 0 |
| **現行 + MOM** | **−16.2567** | **3.89e-01** | 0 |
| **RedCut 撤廃 + MOM** | **−16.2567** | **3.89e-01** | 0 |

- **MOM だけで E は定着し、gap が 3.5e-6 → 0.389 に回復する**。「gapless 最適解」に
  見えていたのは、アウフバウ占有が誤った枝を選んでいたため
- **RedCut の有無は結果を変えない**(MOM ありの 2 行が完全一致)
- スタック点(jast_seed11272 step 2299)でも: 現行 −15.06 → **MOM −15.98**(最良 −16.04)
- 健全点(nojast_seed11272 step 2000)では **MOM と現行が完全に同一**(−16.2557、
  gap 0.281)。健全域では MOM はアウフバウと同じ占有を選ぶので**回帰リスクが無い**

決定的な 1 step(縮小実験 step 1650、両案で同一):

```
step   E         gap        FS ステップ長  主角
1649  −16.2381  5.215e-06     0.0017      89.991°   ← 占有が入れ替わった
1650  −15.1321  1.370e-05     0.0070       0.201°   ← E が +1.106
```

**FS ステップ長 0.0017 は直前の step(0.0007〜0.0018)と同サイズ**。つまり
**共変トラスト領域 `δ†Sδ ≤ r²` では防げない** — S は無限小変位に対する Φ の変化を
測るので、level crossing での不連続な飛びを予測できない。

### 15-5. §1-f  ドリフトではなく凍結

`‖α_t − α_{t−1}‖` の中央値:

| run | step 0-500 | 500-1000 | 1000-1500 | 1500-2000 | 2000-2299 |
|---|---|---|---|---|---|
| jast_seed777 | 3.2e-2 | 1.2e-2 | 2.8e-3 | 4.5e-3 | **3.5e-8** |
| jast_seed11272 | 2.6e-2 | **4.9e-7** | **4.0e-7** | **3.3e-8** | 4.0e-3 |
| nojast_seed11272(成功) | 2.1e-2 | 5.7e-3 | 2.4e-2 | 1.5e-2 | 1.5e-2 |

**発症すると α は完全に止まる**。E が動いて見えるのは MC ノイズ。したがって
「プラトーに定着できず漂流」(§14)は**「悪い枝に飛ばされた後、凍結して戻れない」**が
正しい。拡散則は成立しない(そもそも動いていない)。カット集合の Jaccard 距離は
中央値 0(チャタリングは常態ではない。ただし瞬間的に 1.0 になる step はある)。

### 15-6. §2  単ステップ再生の順位表(同一サンプル・相関サンプリング)

発症点でのみ案の差が出る。ΔE は保存ウォーカーの相関サンプリング(ESS ≥ 1999.5 /2000)、
差は**同一ウォーカーのジャックナイフ**。

| 点 | gap | RedCut が切る本数 | 案 | \|δθ\| | ΔE − ΔE_current |
|---|---|---|---|---|---|
| jast777 step 2299 | 3.7e-8 | 82/130 | 1 / 2 | 1.77e-2 | **−0.001275 ± 0.000391** |
| | | | 4 | 1.26e-2 | −0.000680 ± 0.000281 |
| | | | 3(現行) | **1.81e-8** | (基準) |
| jast777 step 2000 | 3.9e-7 | 82/130 | 1 / 2 | 2.94e-2 | **−0.002863 ± 0.000523** |
| | | | 4 | 2.49e-2 | −0.002335 ± 0.000389 |
| | | | 3(現行) | **1.26e-7** | (基準) |
| jast777 step 1000 | 3.2e-5 | 61/130 | 1 / 2 | 9.51e-3 | −0.000244 ± 0.000139 |
| jast777 step 1570 | 9.2e-5 | 0/130 | 全案同一 | 6.56e-3 | 0 |
| jast11272 step 2299 | 2.1e-3 | 0/130 | 全案同一 | 1.11e-1 | 0 |
| nojast11272 step 2000 | 2.6e-1 | 0/96 | 全案同一 | 1.61e-2 | 0 |

- **案 1 と案 2 は数値的に完全同一**(cos = 1.0000、δ も ΔE も一致)。乗法 StaDel と
  均衡化 + 一様 Tikhonov が厳密に同値であることの実証(外部レビュー §0-b-1 の確認)。
  案 2 の価値は**スペクトルがログに出せること**に限られる
- **現行(案 3)の δ は発症点で 1.8e-8 = 事実上ゼロで、案 1/2 と直交**(cos = 0.000)
- 案 4(モード別 S/N)は常に案 1/2 より劣る(過剰減衰。f<0.5 のモードが 90/130)

### 15-7. §2-d  衛生点検 — 桁落ちは無罪、代わりに S の定義差を検出

同一定義どうしの 1 パス / 2 パスの差は **7.5e-16**(|O| が 1.89e8 でも機械精度)。
**桁落ちは起きていない**。

一方で別の食い違いが出た。上流の `build_s_matrix_and_g_vector!` は

```
S_ab = Re⟨O_a O_b*⟩ − Re⟨O_a⟩ · Re⟨O_b⟩
```

と組むが、実パラメータの Fubini–Study 計量は

```
S_ab = Re[⟨O_a* O_b⟩ − ⟨O_a⟩* ⟨O_b⟩] = Re⟨O_a O_b*⟩ − Re⟨O_a⟩Re⟨O_b⟩ − Im⟨O_a⟩Im⟨O_b⟩
```

で、**`Im⟨O_a⟩Im⟨O_b⟩` の項が落ちている**。既存 mVMC では射影 O が実数・f_ij が
正則なので `Im⟨O⟩ = 0` になり差は出ないが、**パートンの MF ブロックは非正則**なので
残る。実測(相対、`max|S|` 基準):

| 点 | 定義差 | ‖Im⟨O⟩‖ / ‖Re⟨O⟩‖ |
|---|---|---|
| jast777 step 2299 | 1.26e-4 | 2.53e5 / 7.25e6 |
| jast777 step 2000 | 5.77e-4 | 6.86e3 / 1.95e4 |
| jast11272 step 2299 | 2.09e-3 | — |
| nojast11272 step 2000 | **1.45e-1** | — |

上流規約(C 版互換)なので**既存 run の再現性に関わる**。本作業では変更しない。
別件として記録する(§8 の等式テストは力ベクトルを見ており、S のこの項は検査範囲外)。

条件数については外部レビューの見立てどおり: `cond(C) = 5.9e17` でも
**Tikhonov 後は 4.6e3**。線形代数は律速ではない。

### 15-8. 機構の確定

| 説 | 判定 | 根拠 |
|---|---|---|
| 条件数説 | **棄却** | Tikhonov 後 cond(C) = 4.6e3、相対誤差 ~1e-14 ≪ MC 雑音 |
| セクター説 | **適用外** | H_MF が並進非対称・充填が非整数で Chern 数が定義できない |
| RedCut 説(§14) | **症状** | 撤廃しても軌跡が変わらない(15-4 の表)。役割は発症後の凍結 |
| **枝の乗り換え説** | **採択** | 主角 89.99° と E +1.1 が同期。MOM で E −16.2567・gap 0.389 に回復 |

確定した連鎖:

```
最適解が「下から Ne 個」以外の占有を要求している(非アウフバウ)
  → SR が E を下げようとすると gap が閉じる(corr(E, log gap) > 0)
  → gap ≪ ‖δα‖ になり、計量的に小さい 1 ステップで準位順序が入れ替わる
  → 占有スライスが不連続に飛ぶ(主角 90°)= 別の枝へ乗り換え、E が +1.1
  → 飛んだ先でも gap は潰れたまま ∂Φ ∝ 1/gap が S を 1e14 に押し上げ
  → RedCut の相対閾値が健全方向を 82/130 カット → δα ~ 1e-8 で α が凍結
  → 戻れない
```

### 15-9. 推奨と不採用理由

| 優先 | 対処 | 根拠 | 注意 |
|---|---|---|---|
| **1** | **占有の連続追跡(MOM)または占有集合の明示指定** | 唯一 E を定着させる(15-4)。健全域では現行と完全一致 | **ansatz の定義変更**。α → Φ が履歴依存になる。リスタート時に前ステップの Φ が要る |
| 2 | RedCut の置換(死床 + 均衡化 + Tikhonov = 案 2) | 発症後の凍結を防ぐ。勾配式に触れないので §8 の等式テスト群が無傷 | 単独では救済にならない。MOM との併用が前提 |
| — | 共変トラスト領域 `δ†Sδ ≤ r²` | **不採用**。乗り換えは FS 計量で通常サイズの step で起きる(15-4) | — |
| — | 絶対閾値 | **不採用**。テンプレート T_k ごとの自然スケール差で壊れる | — |
| — | 分母正則化 `D/(D²+ε²)` | **不採用**(外部レビュー確定。目的関数を平滑化せずに勾配だけ差し替えるのは誤り) | M3 候補として記録のみ |
| — | 統計的死床 / median 床 | 発火しない(切る本数 0)。保険として残す価値はある | — |

### 15-10. 実装に進む場合の設計メモ

**A. 占有追跡(本命)**

- 選択規則: `occ = argmax_{|occ|=Ne} Σ_{i∈occ} ‖Φ_prev† φ_i‖²`(貪欲で厳密)
- 固有対を (占有 | 非占有) に並べ替えてから契約 0′ を呼べば、**摂動論の式は無変更**で
  よい(分母は並べ替え後の ε の差)。診断実装で確認済み
- **min_gap は符号を持ちうる**(占有準位が非占有より高い = 励起配置)。既存の
  `@warn`・`gap_tol` clamp は絶対値で判定しているのでそのまま使える
- 履歴依存の扱い(要設計判断): (a) Φ_prev をランタイム状態として持つ / (b) MOM で
  到達した占有集合を `.def` に固定出力し、以後は準位番号ではなく固定集合で読む。
  **(b) なら再現性が α だけで閉じる**
- トリガ: 常時 ON でよい(健全域では現行と一致することを実測済み)。ただし既定 OFF +
  無次元トリガ(`min_gap < κ · median(|ε_i − ε_j|)` など)にすれば v3.11 とビット一致を保てる
- MPI: 選択は α と Φ_prev から決定論的に決まるので追加通信は不要(ゲージ射影と同じ位置)

**B. RedCut の置換(併用)**

- 既定 OFF で v3.11 とビット一致。発火は無次元トリガ `max(S_aa)/median(S_aa) > 1e6`
  (実測: 健全点 38〜73、発症点 1.6e14 なので分離は明瞭)
- パイプライン: 死床(統計 `S_aa < κ·SE`、既定 κ=2)→ 均衡化 `C = D^{-1/2} S D^{-1/2}`
  → 一様 Tikhonov(既存 StaDel と同値)→ 解く → 逆変換
- 案 2 は案 1 と同値なので、**実装するなら固有分解版(案 2)にしてスペクトルを
  SRinfo に出す**のが情報量で勝る
- modpara キーを足すなら DESIGN §3.1 の 3 登録点 + マーカー規律

**C. テスト計画**

- 既定(全対処 OFF)で v3.11 とビット一致する E2E(zvo_out / SRinfo / pmfpara_opt / var)
- MOM: 健全なフィクスチャでアウフバウと占有集合が一致すること / 人工的に交差を
  作った 2 準位系で枝を連続に追うこと
- 案 2: RedCut 相当の入力で現行と同じ解を返すこと(死床が発火しない領域)
- §8 の等式テスト群(8-2/8-4/8-7/8-8/8-14/8-15/8-16)は A・B とも勾配式に触れないので無傷

### 15-11. 再現手順

```
JL=~/.julia/juliaup/julia-1.10.2+0.x64.linux.gnu/bin/julia
PG=<repo>/playground_nozomi          # main 側の作業ディレクトリ
cd <worktree>

# 第 0 段: 全 SR ステップの遡及診断
$JL --project=MVMCOptimizers.jl $PG/diag_sr/replay_trajectory.jl \
    $PG/parton_m2_final/jast_seed777_in $PG/parton_m2_final/jast_seed777_out \
    $PG/diag_sr/out/jast_seed777
python3 $PG/diag_sr/analyze_step0.py

# 第 1 段: 単ステップ再生(4 案 + 相関サンプリング)
$JL --project=MVMCOptimizers.jl $PG/diag_sr/onestep_replay.jl \
    $PG/parton_m2_final/jast_seed777_in $PG/parton_m2_final/jast_seed777_out \
    2299 $PG/diag_sr/out/onestep_drift_late

# 縮小実験(variant: current / nocut / balanced / snr / trust、末尾 _mom で MOM)
$JL --project=MVMCOptimizers.jl $PG/diag_sr/multistep_replay.jl \
    $PG/parton_m2_final/jast_seed777_in $PG/parton_m2_final/jast_seed777_out \
    1570 400 current_mom $PG/diag_sr/out/multi3
```

---

## 16. 占有追跡(MOM)の実装と 3 シード検証(v3.12、2026-08-14)

§15 で確定した機構への対処として、**占有集合を状態の一級市民にした**
(`PartonOccMode`)。既定は `0 = aufbau` で v3.11 と**ビット一致**する opt-in。

### 16-1. 実装の要点

| 対象 | 内容 |
|---|---|
| `PartonOccMode` | 0 = aufbau(既定、v3.11 と同一)/ 1 = mom。2 以上は門番が拒否 |
| 選択則 | `score_m = ‖Φ_ref† φ_m‖²` の上位 Ne 本。**部分空間の重なり**で測る(個別軌道の一対一対応ではない)。フレーバーごとに独立 |
| タイブレーク | スコア降順 → 固有値昇順 → band index 昇順(**決定論的**。同一入力でビット再現) |
| 契約 0 / 0′ | `1:Ne` / `Ne+1:NSite` の仮定を**占有集合 O 経由**に置換。`U_occ` / `U_unocc` も O で分割 |
| `min_gap` | 「占有集合の最大固有値と非占有集合の最小固有値の差」= **符号つき**。aufbau では従来の HOMO-LUMO と一致し、非アウフバウ占有では負を取りうる |
| 出力 | `.def` 族 `zqp_pmfocc_{init,opt}.dat`(`NPmfOcc`、`flavor band_index`)。`zqp_pmfband_opt.dat` の `occupied` 列も実際の占有に |
| 診断列 | `zvo_parton_diag.dat` に `n_occ_deviation` と `principal_angle_max` |
| 終端検査 | `occ_selfcontained` / `occ_gap_ok` / `occ_aufbau_ok` / `occ_n_deviation` / `occ_min_gap` を `zvo_parton_runinfo.dat` に記録 |

**既定経路の演算列を変えないための実装上の注意**(v3.12 で実測):
`@view U[:, occ]` を `Vector{Int}` で作ると**非連続 view** になり、`mul!` が BLAS gemm
から generic matmul へ落ちて総和順が変わる。値は同じでも **E の最終桁が 1e-15 ずれた**。
そこで aufbau のときは添字を `UnitRange` のまま渡す 2 経路にしてある
(`_parton_is_aufbau` → `_parton_derivatives_flavor!` の特殊化)。

### 16-2. 既定のビット一致(§7-1)

変更前の worktree(HEAD = v3.11)と**同じ入力**を回して出力をバイト比較した:

```
git worktree add --detach <baseline> HEAD          # 変更前の状態
# SFMT.jl / PfaPack.jl はサブモジュールなので worktree からシンボリックリンク
julia --project=<baseline>/MVMCOptimizers.jl <script> <in> <out_base>
julia --project=<worktree>/MVMCOptimizers.jl <script> <in> <out_new>
```

| 分類 | ファイル | 結果 |
|---|---|---|
| 数値・再現 | `zvo_out.dat` / `zvo_var.dat` / `zvo_SRinfo.dat` / `zvo_conv.dat` / `zqp_opt.dat` / `zqp_pmfpara_{init,opt}.dat` / `zqp_pmfham_opt.dat` / `zqp_pmfband_opt.dat` | **全てバイト一致** |
| 診断 | `zvo_parton_diag.dat` | 既存 9 列は完全一致(追加 2 列のみの差) |
| メタ | `zvo_parton_runinfo.dat` | `PartonOccMode` と `occ_*` 5 行の追加のみ |
| 計時 | `zvo_CalcTimer.dat` / `zvo_parton_time.dat` | 壁時計なので元から非決定的 |

### 16-3. テスト(§8-17-3 〜 17-13)

`MVMCOptimizers.jl/test/test_parton_occmode.jl`(新規)。TDD で 1 項目ずつ
RED → GREEN を確認した。**Parton Mode Tests 2706 pass / Unit Tests 793 pass、既存全緑**。

パーサ側の `test_utils.jl:81`(`@test isfile("README.md") == false`)は **baseline でも
同じく落ちる**実行ディレクトリ依存の既存テストで、本作業とは無関係。

### 16-4. 3 シード検証(§7-3) — **2/3 合格。1 シードは別機構**

CB ν=1/2 / transJastrow / 拡大セル (2,0) / Kx=Ky=0 / nqp=2 / NVMCSample=2000 /
2300 step / コールドスタート。

| seed | mode | E_best | E_tail100 | 漂流幅 | gap 末尾 | 主角 max | 判定 |
|---|---|---|---|---|---|---|---|
| 777 | aufbau | −16.2603 | −15.5852 | **0.675** | 3.70e-08 | **90.00°** | × |
| 777 | **mom** | **−16.2713** | **−16.2504** | **0.021** | −2.65e-01 | **1.10°** | ○ |
| 11272 | aufbau | −15.0931 | −14.7493 | 0.344 | 1.59e-03 | 89.99° | × |
| 11272 | mom | −14.3860 | −14.2794 | 0.107 | −7.69e-03 | 4.25° | × |
| 4321 | aufbau | −16.2906 | −16.2676 | 0.023 | 8.01e-01 | 0.99° | ○ |
| 4321 | **mom** | −16.2906 | −16.2676 | 0.023 | 8.01e-01 | 0.99° | ○ |

判定は「E_tail100 ≤ −16.23(nojast ベースライン)かつ 漂流幅 ≤ 0.1」。

- **seed 777(漂流の典型例)で目的を達成**: E_tail100 が −15.5852 → **−16.2504**、
  漂流幅 0.675 → 0.021。**主角が 90° → 1.10°** になり、枝の乗り換えが機構的に消えた
- **seed 4321(健全)は aufbau と完全一致**。健全域では MOM がアウフバウと同じ占有を
  選ぶという性質の実測(回帰リスクが無いことの担保)
- **主角 > 45° の step は MOM では 0**(aufbau では 777 で 2 回、11272 で 5 回)

**seed 11272 は MOM でも未達で、しかも aufbau より 0.47 悪い**(−14.2794 vs −14.7493):

```
        step:    0    200    400    600    800   1000  …  2200
aufbau   E:  -0.99 -12.75 -14.08 -13.01 -14.19 -12.98  … -14.37   gap 1e-7 台で振動
mom      E:  -0.99 -12.75 -14.08 -14.17 -14.26 -14.26  … -14.30   gap −7.7e-3 で定着
```

両モードは **step 400 まで完全に同一**で、そこから分岐する。この時点で既に E ≈ −14.1 =
**777 / 4321 の −16.2 とは別の盆地**に入っている(REPORT §12-4 の盆地問題)。

- MOM は「その盆地の中で」安定化に成功している(漂流幅 0.344 → 0.107、主角 89.99° → 4.25°)
- しかし **aufbau の枝の乗り換えは、悪い盆地から偶然抜け出す働きもしていた**
  (E_best −15.09 は step 2295 の乗り換え直後)。MOM は連続な枝を追うので
  その脱出経路を封じる
- **これは MOM の本質的なトレードオフ**: 漂流を止める代わりに、乗り換えによる
  探索も止める。§8 のエスカレーション条件「別の機構が残っている」に該当し、
  残っている機構は**盆地選択(初期値・アニーリング)**であってソルバではない

### 16-5. 終端の自己完結性検査(§2-c)— **explicit の要否を決める測定**

| seed | mode | `occ_selfcontained` | `occ_n_deviation` | `occ_min_gap` | dev>0 の step |
|---|---|---|---|---|---|
| 777 | aufbau | 1 | 0 | 3.69e-08 | 0/2300 |
| 777 | **mom** | **0** | 1 | **−0.2648** | **1791/2300** |
| 11272 | aufbau | 1 | 0 | 1.14e-03 | 0/2300 |
| 11272 | **mom** | **0** | 1 | **−0.0077** | **1544/2300** |
| 4321 | aufbau | 1 | 0 | 0.7984 | 0/2300 |
| 4321 | mom | 1 | 0 | 0.7984 | 0/2300 |

**非アウフバウ最適解は本物だった**。MOM が定着した状態は、777 で全 step の 78%、
11272 で 67% が「下から Ne 個」ではない占有で、終端でも `occ_min_gap` が負
(占有準位が非占有準位より高い = 励起配置の Slater 行列式)。§1-e で立てた仮説
「最適解が非アウフバウ占有を要求している」の直接証拠。

- したがって **`_pmfocc_opt.dat` を添えないと α\* から状態を再現できない**。
  警告は実際に出る(`occ_selfcontained = 0` の run)
- `aufbau` の run はすべて `occ_selfcontained = 1` だが、seed 777 の
  `occ_min_gap = 3.69e-08` は「バンドが分離していない」ので、**形式的に成立していても
  実用的には α\* から占有を復元できない**(対角化しても HOMO/LUMO が数値的に縮退)。
  この意味でも MOM の方が状態として健全
- **`InPmfOcc`(占有を入力で固定する経路)の必要性が実測で示された**。ただし占有は
  出力に記録済みなので情報は失われておらず、実装は次の作業に回せる

### 16-6. 判断と次の作業

- **採用**: 占有追跡は「枝の乗り換えによる漂流」に対する根治として機能する(seed 777、
  主角 90° → 1.1°)。健全域では現行と完全一致するので既定 ON にしても回帰しないが、
  v3.12 では **opt-in のまま**にしてある(非アウフバウ占有という物理的な含意が大きいため)
- **RedCut の置換は入れていない**(DESIGN §10)。MOM 有効時の発火は 0/400 step、
  撤廃単独では軌跡が変わらない = 現時点で測定可能な利得がゼロ
- **残った課題(スコープ外)**: seed 11272 の盆地問題。MOM は盆地内の安定化しかしない。
  必要なのは初期値・アニーリング・ウォームスタート(§13 で実証済み)といった
  盆地選択の手当てで、ソルバの修理ではない
- **次に作るなら `InPmfOcc`**(§16-5)。`_pmfocc_opt.dat` をそのまま読み戻せる形にする

---

## 17. `InPmfOcc`(占有集合の読み戻し)の実装(v3.13、2026-08-14)

§16-5 の終端検査が `occ_selfcontained = 0` を返した — **非アウフバウ最適解は本物**で、
`PartonOccMode = 1` の run は α\* 単独では状態を再現できない。占有は
`zqp_pmfocc_opt.dat` に記録済みだが**読み戻す経路が無かった**ので、それを埋めた。

実務上の穴だったもの: ウォームスタート / 鎖方式(α だけ渡すと別状態から始まる)、
PhysCal(M3)で最適化済み状態を測定できない。バンド / Chern 解析は
`_pmfocc_opt.dat` を直接読めば済むので影響なし。

### 17-1. 実装

| 対象 | 内容 |
|---|---|
| スイッチ | `namelist.def` に `InPmfOcc` の行があるかどうか。**modpara にキーは足さない**(`InPmfPara` と同じ作法) |
| 形式 | `zqp_pmfocc_{init,opt}.dat` と**同一**(`.def` 族、5 行ヘッダ、`NPmfOcc`、`flavor band_index` の 0-based)。**出力をそのまま渡せる** |
| 効き方 | **初期占有としてのみ**。以後は `PartonOccMode` に従う(0 なら次 step から選び直し、1 ならこれが最初の参照になって枝を追う) |
| リーダ | 既存の汎用ヘルパ(`read_def_file` / `clean_line` / `split_def_line`)を組み合わせた最小の 1 関数。**新規パーサファイルは作らない** |
| 検証 | 門番 `validate_parton_occupation`。行数 / 範囲 / フレーバーあたり本数 / 重複 / ヘッダ件数を計算開始前に検査し、破れたらエラー停止(**部分適用しない**) |
| 適用点 | ドライバの確定順序(§2.3.1): 乱数 → InPmfPara → OptFlag → 門番 → **初期占有(InPmfOcc があればそれ)** → gauge_target_norm → SR ループ |

**`parse_input_parameter_file` は流用できない**(`idx Re Im` の 3 列専用で
`Dict{Int,ComplexF64}` を返す)。ただし新規パーサも不要で、汎用ヘルパの組み合わせで
足りた。形式は変えていない。

`parton_update_orbitals!` に `forced_occ` キーワードを足し、選択則を丸ごと上書き
できるようにした(占有の格納先は増やしていない — 引数で渡すだけ)。

**`explicit`(全 step 占有を固定 = `PartonOccMode = 2`)は実装していない。**
ウォームスタート / 鎖方式には初期占有の読み戻しで足りる。値 2 は予約のままで
門番が拒否し続ける。

### 17-2. 検証

**§4-1 ビット一致**: `InPmfOcc` を与えない run を v3.12(`c2431f5`)と比較した。
`zvo_out` / `zvo_var` / `zvo_SRinfo` / `zvo_conv` / `zvo_parton_diag` / `zqp_*`
(`pmfocc_{init,opt}` を含む)が**全てバイト一致**。差分は `zvo_CalcTimer` /
`zvo_parton_time`(壁時計)と runinfo の githash・時刻のみ。

**§4-2 往復(本命)**: `PartonOccMode = 1` の run の `zqp_pmfpara_opt.dat` と
`zqp_pmfocc_opt.dat` を `InPmfPara.def` / `InPmfOcc.def` として継続 run に渡すと、

- 継続 run の `zqp_pmfocc_init.dat` が 1 本目の `zqp_pmfocc_opt.dat` と一致
- 両者の初期 Φ が**ビット一致**(`(α*, O*)` の組で状態が閉じた)

**§4-3 非アウフバウ占有の往復**: 「下から Ne 個」でない占有を渡すと、aufbau 初期化では
到達できない Φ が再現される(同じ α でも aufbau は別の Φ を作る)ことを確認。

**§4-4 検証の発火**: 行数不一致 / band 範囲外 / flavor 範囲外 / 重複 /
フレーバーあたり本数不一致 / ヘッダ件数の食い違いの 6 ケースすべてでエラー停止。

**§4-5 init ダンプ**: `InPmfOcc` 由来でも `_pmfocc_init.dat` に実際の初期占有が残る。

**§4-6 aufbau との併用**(挙動の記録): `PartonOccMode = 0` + `InPmfOcc` では、
初期占有は `InPmfOcc`(非アウフバウ)だが**終端は aufbau に選び直されている**。
仕様どおりの挙動で、拒否はしていない — 「aufbau で回すが初期状態だけ前 run から
引き継ぐ」という使い方が成立するため。

**§4-7 回帰**: Parton Mode Tests 2733 pass(v3.12 の 2706 から +27)/ Unit Tests
793 pass / Slater Update 25 pass。既存全緑。

### 17-3. 残っているもの

- `explicit`(全 step 占有固定): 必要になってから。値 2 は予約
- 盆地選択の手当て(REPORT §16-4 の seed 11272): MOM のスコープ外で、初期値・
  アニーリング・ウォームスタートの領域。**`InPmfOcc` はその鎖方式の土台になる**
